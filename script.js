function getComputerChoice() {
    // create a new integer variable for a random number & set it to a random number between 0 and 2
    // IF the variable equals 0, return "rock"
    // IF the variable equals 1, return "paper"
    // IF the variable equals 0, return "scissors"
}